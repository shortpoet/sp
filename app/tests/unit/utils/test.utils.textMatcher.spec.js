// import textMatcher from '@/utils/myTest/test.utils.textMatcher'

// import propFinder from '@/utils/myTest/test.utils.propFinder'
// import propsFinder from '@/utils/myTest/test.utils.propsFinder'
// import propMocker from '@/utils/myTest/test.utils.propMocker'
// import propsMocker from '@/utils/myTest/test.utils.propsMocker'
// import createWrapper from '@/utils/myTest/test.utils.createWrapper'

// // jest.mock('@/utils/myTest/test.utils.createWrapper')
// jest.mock('@/utils/myTest/test.utils.propsFinder')
// jest.mock('@/utils/myTest/test.utils.propFinder')
// jest.mock('@/utils/myTest/test.utils.propsMocker')
// jest.mock('@/utils/myTest/test.utils.propMocker')

describe('test.utils.textMatcher', () => {

  beforeEach(() => {
  })

  // const prop = 'name'
  // const selector = 'h1'
  // const props = ['name']

  // const component = {}

  // let isShallow

  it("calls createWrapper and mocker functions when mock set to false", () => {

    // textMatcher(component, props, prop, selector, false)

    // expect(propsFinder).toHaveBeenCalledWith(props)

    // expect(createWrapper).toHaveBeenCalledWith(component, propsFinder(props))

  })

})


