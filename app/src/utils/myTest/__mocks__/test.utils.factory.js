const factory = jest.genMockFromModule('@/utils/myTest/test.utils.factory')

console.log('hello from factory mock module')

// const createWrapper = jest.fn()

// factory.createWrapper = createWrapper

module.exports = factory